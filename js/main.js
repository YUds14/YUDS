const swiper = new Swiper('.hotel-slider', {
    // Optional parameters
    loop: true,
  

  
    // Navigation arrows
    navigation: {
      nextEl: '.hotel-slider_button--next',
      prevEl: '.hotel-slider_button--prev',
    }
  
  });